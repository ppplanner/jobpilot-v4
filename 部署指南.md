# 🚀 JobPilot v2 — 公网部署指南

> 让全网任何人都能访问你的 JobPilot，无需公网 IP，完全免费

---

## 📊 架构说明

```
用户浏览器
    ↓ 访问
Vercel（前端，永久免费公网域名）
    ↓ API 代理
Railway（后端，免费套餐500小时/月）
    ↓ 数据
SQLite 数据库（随后端一起部署）
```

---

## 第一步：推送代码到 GitHub

### 1.1 在 GitHub 创建仓库

1. 打开 https://github.com/new
2. 仓库名：`jobpilot-v2`
3. 选择 **Private**（私有，保护你的代码）
4. 点击「Create repository」

### 1.2 推送代码

在项目根目录（`f:\vibe coding\jobpilot-v2`）打开命令行：

```bash
git remote add origin https://github.com/你的用户名/jobpilot-v2.git
git branch -M main
git push -u origin main
```

---

## 第二步：部署后端到 Railway

Railway 提供免费的 Python 后端托管，每月500小时免费。

### 2.1 注册并部署

1. 打开 https://railway.app（用 GitHub 登录）
2. 点击「New Project」→「Deploy from GitHub repo」
3. 选择 `jobpilot-v2` 仓库
4. Railway 会自动检测到 Python 项目

### 2.2 配置部署设置

在 Railway 项目设置中：

**Root Directory（根目录）：**
```
backend
```

**Start Command（启动命令）：**
```
uvicorn main:app --host 0.0.0.0 --port $PORT
```

### 2.3 配置环境变量

在 Railway 的 Variables 页面添加：

| 变量名 | 值 | 说明 |
|--------|-----|------|
| `LLM_API_KEY` | `sk-你的API Key` | DeepSeek/OpenAI Key |
| `LLM_BASE_URL` | `https://api.deepseek.com` | API 地址 |
| `LLM_MODEL` | `deepseek-chat` | 模型名称 |
| `LAN_MODE` | `true` | 允许跨域访问 |

### 2.4 获取后端地址

部署完成后，Railway 会给你一个地址，格式如：
```
https://jobpilot-v2-production.up.railway.app
```

📝 **记下这个地址，下一步要用**

---

## 第三步：部署前端到 Vercel

Vercel 专为 Next.js 设计，永久免费，自动 HTTPS。

### 3.1 注册并部署

1. 打开 https://vercel.com（用 GitHub 登录）
2. 点击「Add New Project」
3. 选择 `jobpilot-v2` 仓库
4. **重要**：设置 Root Directory 为 `frontend`

### 3.2 配置环境变量

在 Vercel 的 Environment Variables 中添加：

| 变量名 | 值 |
|--------|-----|
| `BACKEND_URL` | `https://jobpilot-v2-production.up.railway.app`（你的Railway地址）|

### 3.3 部署

点击「Deploy」，等待约2分钟，完成后你会得到：
```
https://jobpilot-v2.vercel.app
```

🎉 **这就是你的永久公网地址，发给任何人都能访问！**

> ✅ 已部署成功：**https://jobpilot-v2-sepia.vercel.app**

---

## 第四步：测试验证

访问 `https://jobpilot-v2.vercel.app`，确认：
- ✅ 页面正常加载
- ✅ 首页数据可以获取（投递统计等）
- ✅ 设置页面可以保存 API Key
- ✅ 简历工作台可以上传文件和生成改写

---

## 🔄 后续更新代码

每次修改代码后：

```bash
cd "f:\vibe coding\jobpilot-v2"
git add .
git commit -m "你的更新说明"
git push
```

推送后：
- **Vercel** 自动重新部署前端（约1分钟）
- **Railway** 自动重新部署后端（约2分钟）

---

## ⚡ 临时方案：localtunnel（当前已运行）

如果只是临时分享测试，当前已用 localtunnel 创建了隧道：

**临时地址**：`https://stale-catfish-52.loca.lt`

> ⚠️ 注意事项：
> 1. 首次访问会出现「访客验证」页面，点击「Click to Submit」按钮即可
> 2. 此地址每次重启 lt 都会变化
> 3. 你的电脑关机后地址失效

**重新获取临时地址**（每次重启后运行）：
```bash
lt --port 3000
```

---

## 📱 分享给同学的消息模板

```
【JobPilot 求职助手 — 邀请测试】

🔗 访问地址：https://jobpilot-v2.vercel.app

这是我做的一个AI求职辅助工具，主要功能：
📋 简历改写：拖拽上传简历+JD，AI对比改写
📊 投递看板：记录所有投递进度
🎯 刷题备考：产品经理面试题库
📸 支持JD截图粘贴（截图后Ctrl+V直接识别文字）

需要在「设置」页面填写自己的 DeepSeek API Key 才能使用AI功能
（申请地址：https://platform.deepseek.com，充值10元够用很久）

欢迎体验反馈！🙏
```

---

## 💡 常见问题

**Q: Railway 免费额度用完了怎么办？**
A: 每月重置。或者充值 $5/月，有 $5 免费额度，够小项目长期使用。

**Q: Vercel 有使用限制吗？**
A: 免费版每月100GB流量，个人项目完全够用。

**Q: 数据存在哪里？**
A: SQLite 存在 Railway 服务器上。注意：Railway 免费版服务重启后数据可能丢失，建议定期导出。生产环境建议换成 PostgreSQL（Railway 免费提供）。

**Q: 如何自定义域名？**
A: 在 Vercel 项目设置 → Domains 中添加你的域名（需要在域名服务商配置 CNAME 记录）。
