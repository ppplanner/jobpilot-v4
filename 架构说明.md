# JobPilot 架构说明(产品经理友好版)

> 面向不懂代码的产品/运营同学,用生活化比喻讲清系统怎么搭、数据怎么存、为什么这么设计。
> 阅读顺序:Level 1 → 4 逐层加深。数据库部分已完整;后端逻辑部分待补。

---

## Level 1 · 全局:用开餐厅来理解

整个产品是一个标准的"三层结构":**门店(前端)+ 后厨(后端)+ 仓库(数据库)**,外加一个**外请顾问(AI)**。

| 角色 | 对应技术 | 负责什么 |
|---|---|---|
| 🏪 门店 / 服务员 | 前端 Next.js | 用户看到的界面、点的按钮;只管好看好用,不碰数据加工 |
| 👨‍🍳 后厨 | 后端 FastAPI | 接订单、处理业务逻辑,决定去仓库拿什么、要不要问顾问 |
| 📦 仓库 / 冰箱 | 数据库 Supabase(Postgres) | 存所有数据:简历、投递、题库、经历 |
| 🧑‍🏫 外请顾问 | AI(DeepSeek) | 后厨遇到要动脑的活(改写简历、分析 JD),打电话问他 |

**一个真实流程——点「生成简历改写」:**
1. 你在门店点按钮 → 前端把"简历 + JD"打包成订单
2. 订单送到后端接口 `/api/v1/resume/rewrite`
3. 后端发现需要智能 → 打电话给 DeepSeek,把简历和 JD 念给它听
4. AI 口述改写结果,后端整理好返回界面
5. 你点"保存版本" → 后端把结果放进数据库的 `resume_versions`

**为什么这么分(优势):**
- **各司其职**:界面改版不影响数据;换 AI 模型只要后端改个"电话号码",门店和仓库无感——模型是可插拔的。
- **能独立升级**:用户多了后端可多开;仓库能从本地换成云端(即"云迁移")。
- **数据集中**:统一仓库,好备份、好检索。

---

## Level 2 · 数据库:仓库怎么分格子

**画面**:数据库 = 一排带标签的抽屉柜。每个**抽屉 = 一张「表」**,里面是一张 Excel:**一行 = 一条记录,一列 = 一个字段**。

共 **11 个抽屉**,对应 5 个功能模块:

**① 简历素材库(原材料)**
- `profile_basic` 基本信息 / `profile_internship` 实习 / `profile_project` 项目 / `profile_skill` 技能

**② 简历成品**
- `resume_versions` 针对不同 JD 改写出的版本(含评分、完整快照)

**③ 投递管理**
- `applications` 每条投递 / `application_debriefs` 面试复盘

**④ 刷题备考**
- `interview_questions` 题库 / `question_practice` 练习记录 / `personal_questions` 个人题

**⑤ 日程**
- `custom_calendar_events` 自定义日历事件

**两个关键设计:**
- **A. 抽屉互相指向(关联)**:简历版本可贴标签"我属于第 3 号投递",系统就知道简历对应哪家公司。好处:数据只存一处,改一处全局一致。
- **B. 索引 = 目录卡**:题库多时,按"分类"找题靠索引一查即定位,不用挨个翻。好处:数据越多越快,也为"按习惯推荐"打底。

---

## Level 3 · 抽屉内部:字段与存储设计

**1. 每列都有"类型"**(以 `applications` 为例):`id` 整数自增、`company/status` 文本、`applied_date` 日期、`jd_score` 整数。
类型 = 给数据立规矩(分数列只能放数字、日期列能正确排序),是数据质量第一道防线。

**2. 多值数据"打包成 JSON"**:题库的 `companies` 存的是 `["阿里","字节"]` 这种列表。
- 方案A(规范):另开关联表拆开存——干净但复杂。
- 方案B(打包):压成一串文字塞进一个文本格——省事。本项目选 B。
- 代价:数据库没法直接"精确按公司筛题",要整包读出再判断;题库小没问题,几千道时不便。

**3. 整包快照(反范式)**:`resume_versions.snapshot_json` 把简历改写的整个现场打包成一个 JSON,为了"点历史版本能 1:1 还原界面"。

**设计原则(PM 该懂的判断):**
> 经常要单独查询 / 修改 / 复用的数据 → 拆成规范的独立表;只整体存取、不需单独检索的 → 打包成 JSON 省事。

对照:实习/项目/技能拆开(要单独增改、被 JD 匹配检索);简历快照、题目标签打包(整体存取为主)。本质是"开发速度 vs 检索灵活性"的平衡。

---

## Level 4 · 运行机制与工程设计

**1. SQL = 取货单**:后端不亲自翻仓库,而是写标准格式的取货单(SQL)交给数据库执行。如 `SELECT * FROM applications WHERE status='Offer'`。

**2. 事务:要么全成,要么全不成**:一组操作捆成原子包,全部成功才生效(commit),任一步失败整组撤销(rollback),不留"做一半"的脏数据。本项目 `get_db()` 用 `with` 自动 commit/rollback。

**3. 双模式:一套代码,两种仓库**(工程亮点):靠翻译层 `_DBWrapper` 适配本地 SQLite 与云端 Postgres 的方言差异——

| 差异 | SQLite | Postgres | 处理 |
|---|---|---|---|
| 占位符 | `?` | `%s` | 自动转换 |
| 取新 ID | 自动 | 加 `RETURNING id` | 自动补 |
| 取结果 | 位置或列名 | 默认仅列名 | 加 `_Row` 兼容两种 |

云迁移时 `KeyError: 0`、`datetime() 不存在` 两个 bug,根源都是"代码写了 SQLite 方言,到 Postgres 翻车",修翻译层 + 改方言 SQL 即解决。价值:开发用轻便本地库,上线用云库,代码几乎不改。

**4. 防丢三道锁:**
| 锁 | 机制 | 防什么 |
|---|---|---|
| 提交即落盘 | 弃用 WAL 延迟写,改提交即写主文件 | 进程被强杀丢数据 |
| 启动自动备份 | 每次启动拍快照存 `backups/`,留最近 15 份 | 误删/损坏可回滚 |
| 云端冗余 | 数据在 Supabase | 本机故障、换设备 |

---

## 后端逻辑(待补)

后端"后厨怎么干活"那条线尚未展开,计划包含:请求如何进入、路由如何分工、业务逻辑、与 AI 如何配合、错误如何兜底。需要时补写。
