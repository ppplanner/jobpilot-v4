# JobPilot v4 — 前端产品形态与交互细节说明

> **本文件用途**：给所有 AI（Claude / Cursor / Copilot 等）阅读，让其在不打开源码的情况下，也能准确理解当前前端的**产品形态、页面结构、每个交互细节、配色与文案**。
> **技术栈**：Next.js 16（App Router）+ React + TailwindCSS，纯客户端渲染为主（多数页面 `"use client"`），`/api/*` 经 `app/api/[...path]/route.ts` 代理到 FastAPI 后端（默认 `http://localhost:8000`）。
> **产品定位**：面向「转行 / 应届产品经理求职者」的一站式 AI 求职助手 —— 简历改写、JD 匹配、面试刷题、投递跟踪、求职日历。
> **最后同步日期**：2026-07-01（以 `CLAUDE.md` 改动日志与源码实读为准）。

---

## 目录

1. [全局设计系统（色卡 / 字体 / 圆角）](#1-全局设计系统)
2. [全局框架（侧边栏 / 布局 / 移动端）](#2-全局框架)
3. [数据层（API / localStorage / 代理）](#3-数据层)
4. [页面清单与路由](#4-页面清单与路由)
5. [首页 `/`](#5-首页-)
6. [简历工作台 `/resume`（核心页）](#6-简历工作台-resume核心页)
7. [投递看板 `/tracker`](#7-投递看板-tracker)
8. [素材库 / 档案 `/profile`](#8-素材库--档案-profile)
9. [刷题中心 `/practice`](#9-刷题中心-practice)
10. [JD 分析 `/jd-analysis`](#10-jd-分析-jd-analysis)
11. [求职日历 `/calendar`](#11-求职日历-calendar)
12. [功能导览 `/onboarding`](#12-功能导览-onboarding)
13. [设置 `/settings`](#13-设置-settings)
14. [遗留 / 未渲染组件](#14-遗留--未渲染组件)
15. [交互模式共性总结](#15-交互模式共性总结)

---

## 1. 全局设计系统

设计语言：**低饱和大地色系 + 极简克制**（Linear / Vercel 风格）。定义于 `app/globals.css` 的 `:root`。

### 1.1 CSS 变量（色卡唯一真源）

| 变量 | 值 | 用途 |
|---|---|---|
| `--primary` | `#2E5B54` | 主色·深墨绿（链接、激活态、主按钮） |
| `--primary-bg` | `rgba(46,91,84,0.08)` | 主色 8% 浅底（选中态背景、成果高亮底） |
| `--hero` | `#234B47` | 更深墨绿，Hero / 工作台大卡 |
| `--accent` | `#E7DBC4` | 米杏色，强调按钮 / 点缀 |
| `--accent-dk` | `#C9B996` | 米杏深一档，边框 / hover |
| `--bg` | `#F4F7EE` | 页面底色·极浅暖薄荷 |
| `--surface` | `#FFFFFF` | 卡片底色·纯白 |
| `--surface2` | `#E9EFDD` | 次级面·浅薄荷绿（输入框底、hover） |
| `--border` | `#DBE3CD` | 描边·薄荷灰绿 |
| `--text-main` | `#20302D` | 主文字·墨绿近黑 |
| `--text-sub` | `#4C5C57` | 次文字 |
| `--text-muted` | `#8B998F` | 弱文字 / 辅助 |
| `--primary-old` | `#C17A3B` | 旧橙色别名（遗留兼容，待清理） |

### 1.2 字体
- 正文：`-apple-system, BlinkMacSystemFont, "PingFang SC", "Inter", sans-serif`（body 应用 Geist）。
- 卡通标题字体：`ZCOOL_KuaiLe`（站酷快乐体）→ CSS 变量 `--font-cartoon` + class `.font-cartoon`。**注意：环境无法下载 Google Fonts，卡通字体当前显示 fallback，未真正生效。**

### 1.3 字号 / 字重规范（首页已收敛为 4 档）
- `text-xl` 大标题（简历工作台标题最大）→ `text-base` 卡片标题·数字 → `text-sm` 正文 → `text-xs` 辅助标签。
- 强调统一用 `font-semibold`（正文用默认字重）。

### 1.4 圆角
- 统一 `rounded-xl`（12px，卡片 / 主要容器）与 `rounded-lg`（按钮 / 输入框）；弹窗用 `rounded-2xl`。

### 1.5 容器边距
- 所有内容页统一：`max-w-6xl mx-auto px-4 py-8`。设置页例外（窄表单 `max-w-lg`）。

> ⚠️ **色系遗留待办**：`calendar` 的 `EVENT_STYLE`、`onboarding` 的 `STEPS`、`tracker` 的状态色仍为**硬编码旧色系**（靛蓝 / 橙 / 翠绿等），未走 `var()` 变量，与主色卡分离，是已知待统一项。

---

## 2. 全局框架

### 2.1 侧边栏 `components/Sidebar.tsx`（桌面常驻展开）
- 桌面：`fixed left-0 top-0 z-40 h-screen w-52`（208px），`bg-white border-r border-gray-100 shadow-sm`，`hidden sm:flex`（<640px 隐藏）。
- **顶部用户信息区**：圆形头像 `w-9 h-9 rounded-full bg-[var(--primary)]`（内显名字首字母白字）+ 问候语 `Hi，{name}`（无名字则 `Hi，未来的产品经理`）+ 角色标签行（`RoleMark` 三角+圆点 SVG + `{target_role}`，点击 `<Link href="/profile">`，`hover:underline`）。数据来自 `useProfile()`。
- **导航项（6 个）**：

| 路径 | 标签 | 图标 |
|---|---|---|
| `/` | 今日 | 房屋 |
| `/tracker` | 投递看板 | 看板 |
| `/resume` | 简历工作台 | 文档+笔 |
| `/practice` | 刷题中心 | 灯泡 |
| `/profile` | 素材库 | 人头 |
| `/jd-analysis` | JD 分析 | 放大镜 |

  底部另有 `/settings` 设置（齿轮）。
- **选中态**：`bg-[var(--primary-bg)]` + `text-[var(--primary)] font-semibold` + 右侧 `w-1.5 h-1.5 rounded-full bg-[var(--primary)]` 小圆点；非选中 `text-gray-400 hover:bg-gray-50`。
- **移动端（<640px）**：侧栏隐藏，替换为顶部 sticky 横向滚动导航条（`h-12`，`overflow-x-auto`，右端齿轮进设置）。

### 2.2 主区 `app/layout.tsx`
```tsx
<main className="min-h-screen bg-[var(--bg)] sm:ml-52">{children}</main>
```
- `sm:ml-52` 桌面给侧栏让位；移动端无左边距（靠顶栏）。

### 2.3 `components/Navbar.tsx`
- **可选顶栏组件，layout 默认不加载**（layout 只挂 Sidebar）。含角色色标（`ROLE_COLOR` 映射：B端蓝 / C端粉 / 增长绿 / 数据琥珀 / 平台灰 / 游戏紫 / AI靛蓝 / 默认）。

### 2.4 `lib/useProfile.ts`
- 管理 `{ target_role, name }`；默认 `{ target_role: "产品经理", name: "" }`（保证 SSR/CSR 首屏一致，避免 hydration mismatch）。
- 挂载后：先读 `localStorage.jobpilot_target_role` 快速回显 → 再请求 `GET /api/v1/profile/basic` 更新并回写 localStorage（失败静默）。

---

## 3. 数据层

### 3.1 代理 `app/api/[...path]/route.ts`
- catch-all，把 `/api/*` 转发到 `process.env.BACKEND_URL || 'http://localhost:8000'`，附带原 query string；仅转发 `content-type`/`accept` 头；支持 GET/POST/PUT/PATCH/DELETE。（`next.config.ts` 明确**不用 rewrites**，避免与 App Router 冲突。）

### 3.2 `lib/api.ts` — 所有前端请求封装
基础：`request<T>(path, options)`，默认 `Content-Type: application/json`，非 2xx 抛 `Error(detail)`。

**模块方法总览：**
- `api.applications.*`（9）：`getAll / getStats / create / update / delete / getDebriefs / createDebrief / deleteDebrief / saveJDAnalysis / getCalendar`
- `api.resume.*`（7）：`upload / diagnose / diagnoseStream / getVersions / saveVersion / linkVersion / deleteVersion`
- `api.profile.*`（11）：`getBasic / updateBasic / getInternships / addInternship / deleteInternship / getProjects / addProject / deleteProject / getSkills / addSkill / deleteSkill`
- `api.ai.*`（8+）：`chat / chatStream / parseJD / jdProfile / matchMaterials / generateQuestions / myTags / tagMatch`（另有直接 fetch 的 `/ai/jd-analysis`、`/ai/interviewer-analysis`、`/resume/rewrite`、`/resume/parse-image`）
- `api.calendar.*`（4）：`getAll / create / update / delete`
- `api.practice.*`（7）：`getQuestions / getStats / addRecord / getPersonal / addPersonal / togglePracticed / deletePersonal`

**关键类型：**
```ts
Application { id, company, position, tier, status, applied_date, source, contact, notes, jd_url, jd_score?, jd_summary?, jd_text?, interview_at?, deadline?, created_at? }
MyTag  { id, label, domain: "direction"|"method"|"domain"|"tooling"|"soft", strength: "强"|"弱", materials: {type,id}[], in_jd? }
JdTag  { id, label, domain, priority: "核心"|"重要"|"加分", status: "命中"|"部分"|"缺口" }
TagMatchResult { jd_tags, my_tags, gaps, score, uncatalogued, empty? }
RewriteResult { internships[], projects[], skills[], actions[], raw|null, mode, temperature }
```

### 3.3 全部 localStorage / sessionStorage key

| Key | 值 | 写入 | 读取 | 含义 |
|---|---|---|---|---|
| `jobpilot_target_role` | `{target_role,name}` | useProfile | useProfile | 角色/名字缓存，快速回显 |
| `jobpilot_onboarding_v1_done` | `"1"` | 首页 `dismissGuide` | 首页 | 首次引导弹窗已完成标记 |
| `jobpilot_my_tags` | `MyTag[]` | 首页 | 首页 | 「个人能力」缓存（避免每次调 LLM，可手动刷新） |
| `jobpilot_last_tagmatch` | `{jd_tags,my_tags}` | **resume 页** `persistTagMatch` | 首页右列 | 最近一次 JD 标签匹配结果 |
| `jobpilot_timeline` | `{start_date,target_date}` | 首页 `saveTimeline` | 首页 | 求职时间线（提醒/进度） |
| `jobpilot_notif_{key}` | `"1"` | 首页 `dismissReminder` | 首页 | 提醒条已读（key：`inactivity_*`/`season_*`/`goal_*`/`custom_deadline_*`） |
| `practice_intent_tags` / `practice_intent_cats` | 数组 | 首页 `IntentTagPanel`（当前无触发入口） | practice 页 | 刷题方向传参（session） |
| `jd_analysis_*`（resume/jd/company/position） | 文本 | jd-analysis 页「去改写」 | resume 页 | 跨页带数据 |

---

## 4. 页面清单与路由

| 路由 | 文件 | 行数 | 说明 |
|---|---|---|---|
| `/` | `app/page.tsx` | 1542 | 首页三栏（能力胶囊 / 历史简历 / 日历 + 提醒 + 引导弹窗） |
| `/resume` | `app/resume/page.tsx` | 1730 | **核心**：三步简历适配台 |
| `/tracker` | `app/tracker/page.tsx` | 731 | 投递看板（列表式）+ 侧滑抽屉 + 面试复盘 |
| `/profile` | `app/profile/page.tsx` | 670 | 素材库（4 tab）+ PM 方向选择 |
| `/practice` | `app/practice/page.tsx` | 499 | 刷题中心 |
| `/jd-analysis` | `app/jd-analysis/page.tsx` | 282 | JD 匹配诊断（圆环评分） |
| `/calendar` | `app/calendar/page.tsx` | 313 | 整页求职日历 |
| `/onboarding` | `app/onboarding/page.tsx` | 396 | 8 步功能导览页 |
| `/settings` | `app/settings/page.tsx` | 150 | API Key / 模型配置 |

---

## 5. 首页 `/`

### 5.1 整体布局
- 三栏 `flex flex-col lg:flex-row`（宽屏横排 / 窄屏竖排），在 `max-w-6xl mx-auto px-4` 容器内。
- 左 = 侧栏（全局）；中栏 = 「JD简历适配台」标题 + 工作台 Hero 卡 + 能力匹配面板 + 提醒条；右栏 = 求职路径统计 + 求职日历（`flex flex-col`，路径卡 `lg:flex-1` 撑满使右栏底部与中栏齐平）。

### 5.2 首次打开引导弹窗 `OnboardingGuideModal`（3 页轮播）
- **触发**：`localStorage.jobpilot_onboarding_v1_done !== "1"` 时首屏弹出。
- **三页内容（代码复刻真实界面示意图，非截图）**：
  1. `RewriteMock` — 简历改写双栏对比 + 黄色高亮新增词。
  2. `InterviewerMock` — 面试官视角分析：**总分 78** + 命中/较弱/缺失矩阵 + 追问预警。
  3. `JdAnalysisMock` — JD 分析：圆形评分环 **72** + 逐项匹配 + 必须补充/可突出。
  内容用真实示例（城乡规划转 PM），关键信息脱敏为「某规划设计院」「某互联网大厂」。
- **按钮**：第 1、2 页为「跳过 | 下一步」；**第 3 页变「立即开始 →」**（`<Link href="/resume" onClick={onClose}>`）跳简历工作台。顶部有进度条。

### 5.3 能力匹配面板（能力胶囊）
- 左右两列，卡片高度 `lg:h-[260px]`（底部与右栏日历卡精确齐平），内层 grid `overflow-y-auto`。
- **左列「个人能力」**：数据来自 `api.ai.myTags()`（缓存 `jobpilot_my_tags`，带刷新按钮）。每项**独占一行**：实色圆角图标方块（领域首字）+ 标题/副标题（领域+经历数）+ 右侧药丸「查看经历」。点击跳 `/profile?focus=<type>-<id>`。
- **右列「所需能力」**：读 `localStorage.jobpilot_last_tagmatch`（由 resume 页写入）。图标/药丸按命中状态着色，命中/部分/缺口排在最前。空态引导去工作台分析 JD。
- **领域配色 `DOMAIN_STYLE`（5 域，大地色）**：方向 `#2E5B54` 墨绿 / 方法 `#5E7E52` 橄榄 / 行业 `#B07A52` 陶土 / 工具 `#5B7E86` 灰蓝绿 / 软素质 `#9C8B70` 驼。
- **命中状态 `STATUS_STYLE`**：命中 `#4F8063` 苔绿 / 部分 `#C0954E` 赭黄 / 缺口 `#B6634A` 陶土红。
- 每列超过 3~5 个折叠为 `view all`（`showAllMine`/`showAllJd`）。

### 5.4 历史简历版本卡
- 卡片窄（`w-40`）、单行横向滚动（`flex overflow-x-auto`，不换行不限数量）。
- 结构：**上部分档图画区 + 下部进度条**。分数三档：0-60 红 / 60-80 琥珀 / 80+ 绿（emoji 占位 🌱/⭐/🏆，未评分 📄，待替换为图片）。
- 点击 → 跳 `/resume?version_id=N`，1:1 还原当时对比界面（有快照）。

### 5.5 求职日历 `HomeCalendar`（首页卡片版）
- 月历网格（`aspect-square` 格子），今天主色高亮，有事件的日期底部圆点 `w-1 h-1 rounded-full`。
- **默认展开当天**明细（`sel` 初始为今天），面板固定高度 150px（事件多了内部滚动，不撑高卡片）。
- **记事本式添加**：底部文本输入框 + 添加按钮（回车也提交），直接写自由日程（如「字节一面 14:00」），type 统一存「日程」。
- 每条事件：前置圆点 + title 全文，hover 出现 `×` 删除（`opacity-0 group-hover:opacity-100` → `api.calendar.delete`）。仅记 custom 事件。

### 5.6 工作台 Hero 卡
- 深墨绿底（`var(--hero)`）+ 白字大标题 + 米杏底墨绿字 CTA + 文档图标装饰。跳 `/resume`。

### 5.7 提醒系统（能力面板下方）
- **通用提醒条** `activeReminders`：`bg-[var(--surface2)]` + emoji 图标 + 文案 + 上下文操作（惰性→去投递看板 / 招聘季→设置时间线）+ `×` 关闭（写 `jobpilot_notif_{key}`）。四类：投递惰性 / 招聘季 / 目标超期 / 截止预警。
- **面试紧急卡** `showPriorityCard`：最近一场 `interview_at` 且 ≤14 天时显示。左侧紧急色条：≤1 天 `#B6634A` 陶土红 / ≤3 天 `#C0954E` 赭黄 / 其他 `#2E5B54` 墨绿；右侧 CTA「查看详情」→ `/tracker`。

### 5.8 求职路径统计
- 显示：简历改写 / 累计投递 / 待面试 / Offer 等数字（真实数据）。

### 5.9 首页其他状态
- `TimelineSettingsModal`：设置开始求职日 + 目标 Offer 日（写 `jobpilot_timeline`），附招聘季图例（秋招 8/1-10/31 / 春招 2/1-4/30 / 实习 3/1-6/15）。
- **注**：当前首页**没有**「新手清单/三步开始」板块（2026-06-29 已移除）。

---

## 6. 简历工作台 `/resume`（核心页）

包裹在 `<Suspense fallback="加载中...">` 中。支持 URL 参数：`?company=` `?position=` `?app_id=` `?version_id=`（直达 Step3 还原历史版本）。

### 6.1 三步步骤指示器
| 步 | 标签 | 语义 |
|---|---|---|
| 1 | 基础简历 | 获取简历内容 |
| 2 | 目标 JD | 粘贴 JD + 配置改写参数 |
| 3 | 生成版本 | 双栏对比 + 面试官评分 + 保存/导出 |
- 当前步 `bg-[var(--primary)] text-white`；已完成可回溯点击（`step > s.num` 才允许跳）；未到 `cursor-not-allowed`。步间 `→` 连接。**只能向前跳回已完成步，不能跳到未到步**。

### 6.2 Step1 — 基础简历（两种来源，`step1Mode`）
**模式 A 上传/粘贴 `ResumeDropZone`**：
- 拖拽区 `border-2 border-dashed`，空态图标 📄（拖拽 `scale-125`）、文案「拖拽简历到这里，或点击选择文件」+「支持 PDF / DOCX / TXT，最大 10MB」。上传中 ⏳「解析中...」。
- `accept=".pdf,.docx,.doc,.txt"`；不支持格式弹「不支持 {ext} 格式...」。上传 `POST /api/v1/resume/upload`（FormData）→ 返回 `{text, char_count}` → `formatResumeText` 格式化。成功绿字「已从「{file}」提取 {n} 字」。
- 有内容后：`textarea rows=14` 可编辑 + 右上「重新上传」「×清空」+ 底部字数。

**模式 B 从素材库导入 `MaterialImporter`**：
- 并行拉实习/项目/技能。空态 📦「素材库还没有内容」+ CTA「去建立素材库 →」(`/profile`)。
- 三区块 checkbox（实习/项目默认全选，技能开关默认开）→「导入所选内容」→ `composeText()` 拼「【实习经历】【项目经历】【技能】」文本 → 自动切回上传模式看结果。

Step1 底部「去匹配 JD →」(`disabled={!resumeText.trim()}`) → `setStep(2)` 并 `syncToLibrary`（防重）。

### 6.3 Step2 — 目标 JD
- **公司/岗位输入**：placeholder「如：字节跳动」「如：B端产品经理」。填公司后显示绿条「✓ 生成后将自动同步到投递看板」。
- **JD 输入 `JDTextArea`**：`rows=8`，支持 **Ctrl+V 粘贴截图 OCR**（`onPaste` → base64 → `POST /api/v1/resume/parse-image` `{image_base64,hint:"jd"}` → 追加文字）。OCR loading 覆盖层「AI 正在识别截图文字...」；模型不支持时琥珀提示「当前模型不支持图片识别，请直接复制JD文字粘贴」。
- **解读 JD `analyzeJD`**（JD>80 字）：按钮「🔍 解读这份JD...」→ `api.ai.jdProfile({jd_text,company,position})`。结果卡（**靛蓝系** `bg-indigo-50`）：PM 类型徽章 + 经验层级徽章 + 核心/重要维度（核心红 `bg-red-100`、重要琥珀 `bg-amber-100`）+ 💡改写重点。**成功后自动 `persistTagMatch()` 写 `jobpilot_last_tagmatch`**。
- **推荐匹配经历**（JD>80 字）：按钮「🎯 推荐匹配经历」→ `api.ai.matchMaterials`。按匹配度排序，每条：匹配度标签（高红/中琥珀/低灰）+ 经历信息 + 理由 + 「＋加入简历」（加入后「已加入 ✓」）。空态引导去素材库。
- **改写模式**：「轻修」（默认，优化表达保风格） / 「精修」（结果先行+压缩STAR，大幅重构）。
- **生成改写 `generate`** → `POST /api/v1/resume/rewrite` `{resume_text,jd_text,mode}` → 存 `result`、算字数、`setStep(3)`、`persistTagMatch()`；**填了公司则自动 `autoSyncToTracker`**（建投递记录 status=意向/tier=匹配/source=简历工作台 + `syncJDAnalysis` 写 jd_score/jd_summary）。

### 6.4 Step3 — 生成版本（双栏对比）
- **顶部状态栏**：公司+岗位 + 模式徽章（轻修蓝/精修橙）+「✓ 已同步到投递看板 查看」+ 投递关联下拉 +「保存版本」+「换一家公司」。
- **双栏对比**：`result.raw` 存在→单 `<pre>`；否则逐条实习/项目双栏（左「原始简历」灰点 / 右「改写结果」`bg-[#FFFDF9]` 主色点）。
- **高亮新增词 `diffHighlight`**：改写文本中不在原文的词用 `<mark bg-yellow-100 text-yellow-900>` 包裹（词级判断，非真 diff）。技能：原技能 `line-through opacity-60`，新技能 `bg-yellow-100` 药丸。
- **字数面板 `WordCountPanel`**：原始 vs 改写字数，增绿/减蓝/持平。
- **AI 改写建议**：`result.actions` 主色圆点列表。

### 6.5 面试官视角评分（Step3 下方，`border-2 border-indigo-200`）
- 按钮「开始分析 / 重新分析」→ `POST /api/v1/ai/interviewer-analysis` `{resume_text:改写后, jd_text, company, position}`。
- 返回 `InterviewerAnalysis`：`overall_score`(0-100) + `summary` + `match_items[]`(strong/weak/missing) + `follow_up_questions[]`(can_answer) + `weak_signals[]` + `strong_signals[]`。
- **评分四档**：≥80 绿「优秀」/ ≥65 琥珀「良好」/ ≥50 橙「合格」/ <50 红「待提升」。依据：`✅N项命中 ⚠️N项较弱 ❌N项缺失`。
- **JD 命中列表**：每条状态图标 + aspect + 状态标签 + JD 要求原文 + `→建议`，背景按状态（绿/琥珀/红）。
- **追问预警**：简历原文「bullet」+ 加粗追问 + can_answer（✓可支撑 / ⚠无法支撑建议补充）。
- **信号词**：两列 —— 强信号词（绿，保留）/ 空话弱信号词（琥珀，`line-through` 建议删）。

### 6.6 保存 / 还原
- **保存版本**：`snapshot_json = {result, jdText, company, position, mode, jdProfile, 字数}`；`analysis_json = 面试官分析`。version_name = `{公司}-{岗位}（轻修/精修）`。成功→「已保存 ✓」+ 刷新历史版本列表。
- **历史版本面板 `VersionHistoryPanel`**（Step3 底部，空则不渲染）：可折叠列表，每条版本名+日期+分数徽章，展开看 summary/命中/追问。
- **还原 `restoreVersion`**（`?version_id=`）：优先用 `snapshot_json` 1:1 还原双栏；无快照的旧版本降级为 `result.raw` 只读。
- **导出 PDF `exportToPDF`**：生成打印 HTML + `window.print()`（当前无独立 UI 按钮触发，函数已实现）。

---

## 7. 投递看板 `/tracker`

### 7.1 形态：纵向**列表式**（非 Kanban 横列）
每条投递是一行卡片（`div.space-y-3`），状态经**下拉切换**而非拖拽。

### 7.2 状态与配色
- **状态 `STATUSES`**：已投递(`#6366f1`靛) / 笔试(`#8b5cf6`紫) / 一面(`#f59e0b`琥珀) / 二面(`#ef4444`红) / HR面(`#10b981`绿) / Offer(`#059669`深绿) / 挂(`#94a3b8`灰) / 放弃(`#94a3b8`灰)。
- **档位 `TIERS`**：冲刺(`#DC2626`红) / 匹配(`#D97706`琥珀) / 保底(`#16A34A`绿)。
- **来源 `SOURCES`**：官网/APP / 内推 / Boss直聘 / 拉勾 / 猎聘 / 牛客 / 其他。
- **复盘轮次 `ROUNDS`**：笔试 / 一面 / 二面 / HR面 / 综合面。

### 7.3 顶部统计（4 卡）
总投递 `total` / 进行中 `active`（排除挂+放弃）/ 面试中 `interviewing`（一面+二面+HR面）/ Offer。

### 7.4 筛选栏
状态下拉 + 档位下拉 + 搜索框（匹配公司/岗位 `includes`），右下「共 N 条」。

### 7.5 投递卡片
- 点击整卡 → 打开侧滑抽屉 `setSelectedApp`。
- 内容：公司 — 岗位 + 状态药丸(20%透明底) + 档位药丸 + 投递日期·来源 + notes 单行截断。
- 右侧（`stopPropagation`）：状态下拉（即时切换，`api.applications.update`）+ `×`删除（`confirm` 后 `DELETE`）。

### 7.6 新增投递弹窗
右上「+ 新增投递」→ `fixed inset-0 bg-black/40` 遮罩 + `rounded-2xl max-w-lg`。字段：公司*/岗位*/档位(默认匹配)/投递日期(默认今天)/截止日期/面试时间(datetime-local)/备注。提交 `api.applications.create` → `loadApps` 全量刷新。

### 7.7 侧滑抽屉 `AppDrawer`（`fixed inset-y-0 right-0 max-w-md`）
- **头部**：公司+岗位 + 状态/档位药丸 + JD 匹配分药丸（≥75绿/≥50琥珀/<50红「JD匹配 N分」）+ 关闭。
- **元信息**：更新状态下拉 + JD 链接 + 笔记。
- **简历版本区**：`linkedVersions`（专属，`border-[var(--primary)]/20`，可展开看 overall_score/summary/match_items≤4/must_add≤2）+ `otherVersions`（折叠「查看其他 N 个版本」≤5）+「针对此投递优化简历」→ `/resume?company=&position=&app_id=`。
- **面试复盘区** `DebriefCard`：轮次标签 + 日期 + 自评星标；被问到的题（每题「+练习」→ `api.practice.addPersonal` 加入题库，变「已加入 ✓」）；没答好的地方（红）；面试官反馈（绿）。
- **添加复盘表单**（暖色 `bg-amber-50`）：轮次下拉 + 日期 + `StarRating`(5星,默认3) + 三个 textarea。

---

## 8. 素材库 / 档案 `/profile`

支持 `?focus=<type>-<id>` 定位。

### 8.1 PM 方向选择器（始终显示，`border-2 border-[var(--primary)]`）
8 个方向按钮 `PM_DIRECTIONS`：B端PM🏢 / C端PM📱 / 增长PM📈 / 数据PM📊 / 平台PM⚙️ / 游戏PM🎮 / AI PM🤖 / 通用PM🧭（每个含 label+desc）。选中 `border-[var(--primary)] bg-[var(--primary-bg)]`。点击设 `basic.target_role`（**需到基本信息 tab 点保存才生效**）。

### 8.2 四个 Tab（`bg-[var(--primary)] text-white` 选中）
1. **基本信息**：8 字段网格（姓名/学校/专业/学历/GPA/毕业时间/目标岗位/目标城市）+ 个人简介 textarea →「保存基本信息」`PUT /profile/basic`，绿条提示 2.5s。
2. **实习经历**：卡片 `id="internship-{id}"`；查看态（公司/岗位·部门/日期 + 高亮内容）；「编辑」→ 内联表单；「删除」红字。底部「添加实习经历」表单（公司*/岗位* 必填）。
3. **项目经历**：结构对称；字段 name*/role*/background/contribution/result/tech_stack；`result` 用主色底强调（`bg-[var(--primary-bg)]`）。
4. **技能标签**：5 分类 `SKILL_CATS`（产品工具/数据分析/编程能力/软技能/行业知识）；技能胶囊按等级配色（精通主色底/熟练边框/了解灰）；等级 `["精通","熟练","了解"]`；新增支持 Enter；**快捷添加**10 个工具按钮（Axure/Figma/Sketch/JIRA/Confluence/SQL/Python基础/Tableau/飞书/Notion，SQL/Python/Tableau 归数据分析，余归产品工具，默认熟练）。

### 8.3 `?focus` 定位高亮（3 秒）
从首页能力胶囊跳来：解析 `type-id` → 校验(internship/project) → 等 `internships/projects` 加载就绪（依赖数组触发）→ 切 tab + `scrollIntoView`(100ms) + 高亮 `border-[var(--primary)] ring-2 ring-[var(--primary)]/40`，3 秒后清除。`focusHandled` ref 保证只触发一次。

---

## 9. 刷题中心 `/practice`

- **数据源**：后端题库（`GET /practice/questions`）+ 统计（`/practice/stats`）+ 个人清单（`/practice/personal`，来自面试复盘「+练习」）。
- **题库分类（6 大类）**：产品 / AI产品 / 数据与增长 / 运营 / 行为面试 / 案例分析。按分类筛选。
- **题目卡片**：题干 + 难度标签（简单/中等/难，四色区分）+ 分类标签 + 答案/解析展开收起。
- **刷题交互**：选题 → 弹窗 → 查看答案 → 自我评估（对错/用时写入 `api.practice.addRecord`）。
- **我的清单**：个人题目用 emerald 色系区分，可切换已练习状态、删除。
- 空态：「暂无匹配题目」「还没有题目」；页面级 loading「加载题目中...」。

---

## 10. JD 分析 `/jd-analysis`

- **定位**：**匹配诊断**（简历 vs JD 差距），区别于 resume 页的 `jdProfile`（岗位画像解读）。
- **输入**：两个独立 textarea（简历 + JD）+ 公司/岗位。可从 resume 页经 `sessionStorage.jd_analysis_*` 带入（显示「← 返回简历工作台」）。
- **分析** `POST /api/v1/ai/jd-analysis` `{resume_text,jd_text,company,position}` → `{overall_score, match_items[], must_add[], highlight[]}`。
- **结果展示**：**圆形评分环** + 逐项匹配列表 + 必须补充 / 可突出。三档语义色（green/amber/red）。JSON 解析失败降级 raw 文本 `<pre>`。
- **导航**：分析后「去简历工作台改写 →」（三处入口均指向 `/resume`）。
- **无持久化**（仅 sessionStorage 传参）。
- **与 resume 页关系**：resume 页 `syncJDAnalysis` 也调 `/ai/jd-analysis`，但用途是把匹配分写入**投递看板**的 `jd_score/jd_summary`，不在工作台内渲染。

---

## 11. 求职日历 `/calendar`

- **布局**：标题栏（`< 月 >` 切换 + 今天按钮 + 招聘季色块指示 + 「+添加事件」）+ 星期行（一~日，周一为首）+ 日历网格（`h-24` 格子）+ 选中日详情面板。
- **网格格子**：今天数字 `bg-[var(--primary)] text-white` 圆圈；选中 `bg-[var(--primary-bg)]`；周末 `bg-[var(--surface2)]/30`；招聘季背景色叠加（秋招橙/春招靛/实习绿）。事件 pill 最多 2 条，超出显 `+N`。
- **事件类型配色 `EVENT_STYLE`**：interview/面试 蓝 / deadline/截止 橙 / offer_deadline 绿 / 备考 紫 / 其他 灰。
- **添加事件弹窗 `AddEventModal`**（`bg-black/30 backdrop-blur-sm` + `rounded-2xl w-80`）：标题输入(自动聚焦,回车提交) + 类型下拉 `EVENT_TYPES=["面试","截止","备考","其他"]` + 时间(time) + 日期(date) →「保存」`api.calendar.create`。
- **详情面板**：选中日显示当天事件（tracker 事件为 `<Link href="/tracker">` / custom 事件带 `×` 删除），空态「当天无事件，点击右上角添加」。
- **数据源**：`api.applications.getCalendar()`（投递事件）+ `api.calendar.getAll()`（自定义事件）。
- **vs 首页 HomeCalendar**：整页有季节指示 / 事件 pill / 类型下拉 / 独立详情面板；首页卡是圆点标记 / 记事本输入 / type 固定「日程」/ 仅 custom 事件。

---

## 12. 功能导览 `/onboarding`

- **形态**：单页轮播式（点击切换，非滚动，无自动播放）。5 段：顶部标题 + 8 步导航条 + 当前步内容（左文右图）+ 全流程一览（8 格网格）+ 底部 CTA 横幅。
- **8 个步骤 `STEPS`**（每步有 emoji/标题/详情/CTA/渐变色/手写 demo JSX 示意图）：
  1. 📋 进入简历工作台 → `/resume`
  2. ⬆️ 上传你的简历 → `/resume`
  3. 📝 粘贴岗位 JD → `/resume`
  4. ✨ 查看 AI 改写结果 → `/resume`
  5. 🔍 面试官视角评判 → `/resume`
  6. 📚 题库刷题备考 → `/practice`
  7. 📊 投递看板跟踪进度 → `/tracker`
  8. 🤖 AI 认识你了 → `/tracker`
- **导航条按钮三态**：当前(主色反白 scale-105) / 已完成(✓) / 未到(灰)。小屏隐藏文字只留圆圈。
- **当前步**：左栏 Step 徽章+标题+desc+detail + 主 CTA（渐变）+「下一步」（末步为「开始使用」）；右栏 `sticky` 功能预览 demo。
- **底部 CTA**：主色横幅「准备好了吗？」+「立即开始」/「返回首页」。
- **⚠️ 与首页 `OnboardingGuideModal`（3 页弹窗）不同**：此为独立整页 8 步导览。

---

## 13. 设置 `/settings`

- 窄表单 `max-w-lg mx-auto px-4 py-12`。3 字段：
  - **API Key**（`type=password`，必填*）：后端 `has_key:true` 时显掩码 `••••`，`onFocus` 检测掩码则清空。提示「支持 DeepSeek / OpenAI / 通义千问」。
  - **API 地址**（默认 `https://api.deepseek.com`）。
  - **模型名称**（默认 `deepseek-chat`）。
- **按钮**：「保存配置」`PUT /api/v1/settings/api-key` → 绿提示「API Key 已保存，立即生效」；「测试连接」`POST /api/v1/ai/chat` 发「你好，请回复「连接成功」」→ 绿提示回显 AI 回复。
- **帮助区**：DeepSeek（约¥0.001/次）/ 通义千问（Base URL `.../compatible-mode/v1`，model `qwen-plus`）外链卡片。

---

## 14. 遗留 / 未渲染组件

`app/page.tsx` 中**已定义但当前 return 不渲染**（CLAUDE.md 约定有意保留，勿删）：
- `FunnelSection` — 求职漏斗进度条（内含 `JobCat` 求职小猫 SVG，6 成长阶段 stage0-5：蓄势待发→初入江湖→广撒网→进入战场→决战时刻→Offer到手）。
- `JobCalendar` — 全功能首页日历（带季节背景、投递/自定义事件、`AddEventModal`）。
- `IntentTagPanel` — 刷题方向多选面板（7 方向），`showIntentPanel` 当前**无任何代码置为 true**，无触发入口。

**未来启用要求**：必须先做整体布局与视觉规划再接入，不要简单堆叠。

---

## 15. 交互模式共性总结

| 维度 | 约定 |
|---|---|
| 数据获取 | 全 `"use client"`，`useEffect` 内 fetch（profile 用原生 fetch，其余多用 `api.*` 封装） |
| 容器 | `max-w-6xl mx-auto px-4 py-8`（设置页 `max-w-lg` 例外） |
| 圆角 | 按钮/输入 `rounded-lg`，卡片 `rounded-xl`，弹窗 `rounded-2xl` |
| 弹窗 | `fixed inset-0 bg-black/30~40 z-40~50` 遮罩，点遮罩关闭，弹窗内 `stopPropagation` |
| 主按钮 | `bg-[var(--primary)] text-white`，禁用 `disabled:opacity-40~60` |
| 加载态 | 按钮内旋转 SVG +「xxx中...」；页面级居中「加载中...」 |
| 成功反馈 | 绿条 `bg-green-50 border-green-200 text-green-700`，多数 2.5~5s 自动消失 |
| 错误反馈 | 红条 `bg-red-50 border-red-200`；AI 类多提示「请检查 API Key 配置」 |
| 空态 | 图标 emoji + 说明 + CTA 引导（如「去建立素材库 →」） |
| 响应式 | 仅 Tailwind `sm:`/`lg:` 断点，无移动端专属抽屉（靠全局侧栏 `ml-52`） |
| 高亮/强调 | 新增词/优化项统一 `bg-yellow-100 text-yellow-900`；成果用主色底 |
| 语义色 | 命中/强=绿，部分/中/警=琥珀，缺口/弱/急=红（大地色低饱和版） |

---

## 附：产品进步路线（团队共识）

三阶路线（不跳级）：
1. **地基**：新手冷启动引导（引导弹窗 / 导览页 / 空态引导）。
2. **留存**：功能闭环 + 进度可视化（可启用遗留的漏斗/求职猫组件）。
3. **深度**：按用户习惯推荐题目 / 更强 AI（依赖题库丰富度 + 刷题行为数据积累）。

> 核心洞察：丰富度问题的本质不是功能少，而是新用户缺「越用越深的路径 + 回来的理由」。
</content>
</invoke>
