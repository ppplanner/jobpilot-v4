# JobPilot v4 — 部署状态与操作手册

> 本文档记录 JobPilot v4 的完整部署现状、各平台账号配置、待解决问题和下一步操作。
> 供在其他电脑/AI 上继续操作时直接读取。

---

## 一、项目概览

| 项目 | 说明 |
|------|------|
| 项目名 | JobPilot v4 |
| 本地路径 | `c:/Users/zhiqiao.wang/Desktop/jobpilot-v4` |
| GitHub 仓库 | https://github.com/ppplanner/jobpilot-v4 |
| 主分支 | `main`（最新 commit: `a47ebbb`） |
| 目标域名 | https://jobpilot.wstudio.cc |

---

## 二、项目结构

```
jobpilot-v4/
├── backend/          # FastAPI 后端（Python）
│   ├── main.py       # 入口，redirect_slashes=False
│   ├── requirements.txt
│   ├── railway.toml  # Railway 部署配置（已废弃）
│   ├── Dockerfile    # Fly.io / Render 用
│   ├── fly.toml      # Fly.io 部署配置
│   └── .env          # 本地环境变量（不提交 git）
├── frontend/         # Next.js 16 前端
│   ├── vercel.json   # Vercel 部署配置
│   └── app/api/[...path]/route.ts  # 代理到后端（GET 6s 超时，非 GET 180s）
├── render.yaml       # Render.com 部署配置
└── 部署状态与操作手册.md  # 本文件
```

---

## 三、各平台部署现状

### 3.1 前端 — Vercel ✅ 已部署

| 项目 | 值 |
|------|-----|
| 平台 | Vercel |
| Vercel 项目名 | `jobpilot-v2`（沿用旧项目，v4 代码已覆盖） |
| Vercel 团队 | `ppplanner-4993s-projects` |
| 生产 URL | https://jobpilot-v2-dn3drcn0k-ppplanner-4993s-projects.vercel.app |
| 自定义域名 | https://jobpilot.wstudio.cc ✅ 已绑定并生效 |
| 部署方式 | `vercel --prod --yes`（从 `frontend/` 目录执行） |
| 最新部署时间 | 2026-07-16 |

**当前问题：**
- `frontend/vercel.json` 里的 `BACKEND_URL` 仍指向旧的 Railway 地址（已失效）：
  ```
  https://jobpilot-v2-production.up.railway.app
  ```
- **需要更新为新后端地址后重新部署前端。**

---

### 3.2 后端 — 待解决 ❌

| 平台 | 状态 | 说明 |
|------|------|------|
| Railway | ❌ 已到期失效 | 旧后端，不可用 |
| Render.com | ❌ 部署失败 | 数据库连接串失效（见下方） |
| Fly.io | ⏸ 未完成 | flyctl 已安装，登录未完成 |

**Render 部署失败原因：**
Supabase 数据库连接串失效，报错：
```
FATAL: tenant/user postgres.iriwstbablwjsqzusmmd not found
```
需要去 Supabase 控制台获取新的连接串。

---

### 3.3 数据库 — Supabase ⚠️ 连接串需更新

| 项目 | 值 |
|------|-----|
| 平台 | Supabase（云端 PostgreSQL 17） |
| 项目 ID | `iriwstbablwjsqzusmmd` |
| 区域 | AWS ap-southeast-1（新加坡） |
| 当前连接串（可能已失效） | `postgresql://postgres.iriwstbablwjsqzusmmd:WZQwzq20020919.@aws-1-ap-southeast-1.pooler.supabase.com:5432/postgres` |

**需要操作：**
1. 打开 https://supabase.com/dashboard
2. 找到项目（ID: `iriwstbablwjsqzusmmd`）
3. 如果显示 **Paused**，先点 **Restore** 恢复
4. 点顶部 **Connect** 按钮 → 选 **Transaction pooler** → 复制 URI
5. 用新连接串更新 `backend/.env` 和 Render 环境变量

---

### 3.4 AI 服务 — DeepSeek

| 项目 | 值 |
|------|-----|
| 平台 | DeepSeek |
| API Key | `sk-f07877a8cb414755a028132649c9da74` |
| Base URL | `https://api.deepseek.com` |
| 模型 | `deepseek-chat` |

---

## 四、后端环境变量（部署时需填写）

部署到任何平台时，需要配置以下 6 个环境变量：

```
LLM_API_KEY=sk-f07877a8cb414755a028132649c9da74
LLM_BASE_URL=https://api.deepseek.com
LLM_MODEL=deepseek-chat
DATABASE_URL=<从 Supabase 获取新连接串>
LAN_MODE=false
ALLOWED_ORIGINS=https://jobpilot.wstudio.cc
```

---

## 五、下一步操作（按顺序）

### Step 1：修复 Supabase 连接串
1. 打开 https://supabase.com/dashboard
2. 进入项目 → 如果 Paused 先 Restore
3. 点 **Connect** → 复制新的 URI 连接串
4. 更新 `backend/.env` 的 `DATABASE_URL`

### Step 2：部署后端（选一个平台）

**方案 A：Render.com（推荐，已有账号）**
1. 打开 https://render.com → 进入已创建的服务
2. 在 **Environment** 标签更新 `DATABASE_URL` 为新连接串
3. 点 **Manual Deploy** 重新部署
4. 等待部署成功，记录后端 URL（格式：`https://xxx.onrender.com`）

**方案 B：Fly.io（flyctl 已安装在本机）**
```bash
# flyctl 路径：C:\Users\zhiqiao.wang\.fly\bin\flyctl.exe
cd c:\Users\zhiqiao.wang\Desktop\jobpilot-v4\backend
flyctl auth login
flyctl launch --no-deploy
flyctl secrets set LLM_API_KEY=sk-f07877a8cb414755a028132649c9da74 LLM_BASE_URL=https://api.deepseek.com LLM_MODEL=deepseek-chat DATABASE_URL=<新连接串> LAN_MODE=false ALLOWED_ORIGINS=https://jobpilot.wstudio.cc
flyctl deploy
```

### Step 3：更新前端 BACKEND_URL

获得后端 URL 后，更新 `frontend/vercel.json`：
```json
{
  "framework": "nextjs",
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "installCommand": "npm install",
  "trailingSlash": false,
  "env": {
    "BACKEND_URL": "https://<新后端URL>"
  }
}
```

### Step 4：重新部署前端
```bash
cd c:\Users\zhiqiao.wang\Desktop\jobpilot-v4\frontend
vercel --prod --yes
```

### Step 5：验证
```bash
# 验证前端
curl -s -o nul -w "%{http_code}" -L https://jobpilot.wstudio.cc/

# 验证后端健康检查
curl -s https://<新后端URL>/health

# 验证 API 代理
curl -s https://jobpilot.wstudio.cc/api/v1/applications
```

---

## 六、Git 操作记录

```bash
# 本地 git 配置（已更新）
user.name = ppplanner
user.email = 1275652889@qq.com

# 推送命令
cd c:\Users\zhiqiao.wang\Desktop\jobpilot-v4
git add -A
git commit -m "描述"
git push origin main
```

---

## 七、已知问题与注意事项

1. **Vercel 部署阻止**：之前提交用的是公司邮箱（`zhiqiao.wang@momenta.ai`），已更新为 `1275652889@qq.com`，需确保 GitHub 账号里添加了这个邮箱。

2. **FastAPI redirect_slashes**：`backend/main.py` 已设置 `redirect_slashes=False`，避免 POST 请求被 307 重定向导致请求体丢失。

3. **前端代理超时**：`frontend/app/api/[...path]/route.ts` 已配置 GET 请求 6s 超时、非 GET 请求 180s 超时（AI 生成任务需要较长时间）。

4. **Supabase 免费套餐**：超过 1 周不活跃会自动 Pause，需手动 Restore。

5. **DeepSeek API Key**：是自己申请的付费 Key，需要账户有余额才能使用 AI 功能。

---

## 八、本地开发启动

```bash
# 后端
cd backend
uvicorn main:app --host 0.0.0.0 --port 8000 --reload

# 前端
cd frontend
npm run dev

# 或一键启动
start-all.bat
```

访问 http://localhost:3000
